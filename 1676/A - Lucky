#include <iostream>
using namespace std;
int main() {
    long long int n;
    cin>>n;
    for (int j=1;j<=n;j++) {
        long long int sum1=0,sum2=0;
        string s;
        cin>>s;
        for (int i=0;i<6;i++) {
            if (i<3)
                sum1+=(s[i]-'0');
            else
                sum2+=(s[i]-'0');
        }
        if (sum1==sum2)
            cout<<"YES"<<endl;
        else
            cout<<"NO"<<endl;
    }
 
    return EXIT_SUCCESS;
}